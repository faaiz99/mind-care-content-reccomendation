// redisClient.js
// import redis from 'redis';

// const client = redis.createClient({
//     host: 'localhost', // replace with your host, if not localhost
//     port: 8082 // replace with your port, if not 6379
// });

// client.on('error', (err) => {
//   console.log(`Error ${err}`);
// });

// export default client;
